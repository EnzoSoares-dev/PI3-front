import React from "react"

export const Input = ({type, required, placeholder, })=>{
    return(
        <input
        className='

        '
        type='text'
        required={required} 
        placeholder={placeholder}/>
    )
}